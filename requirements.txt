# Core
numpy>=1.24
pandas>=2.0

# Optional embedding / LLM backends — install on demand
# ollama>=0.3                            # primary backend (paper baseline)
# sentence-transformers>=2.7            # CPU fallback embedder

# Dev / test
pytest>=8.0
